{{- printf "--- DEBUG: .Values.postgress:\n%s" (toYaml .Values.postgress) | indent 10 }}
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: postgresql
spec:
  serviceName: postgresql
  replicas: 1
  selector:
    matchLabels:
      app: postgresql
  template:
    metadata:
      labels:
        app: postgresql
    spec:
      containers:
        - name: postgres
          image: "{{ .Values.postgress.image.repository }}:{{ .Values.postgress.image.tag }}"
          imagePullPolicy: {{ .Values.postgress.image.pullPolicy }}
          ports:
            - containerPort: 5432
              name: postgres
          env:
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.postgress.postgresql.existingSecret }}
                  key: {{ .Values.postgress.postgresql.usernameKey }}
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.postgress.postgresql.existingSecret }}
                  key: {{ .Values.postgress.postgresql.passwordKey }}
            - name: POSTGRES_DB
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.postgress.postgresql.existingSecret }}
                  key: {{ .Values.postgress.postgresql.databaseKey }}

          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
  volumeClaimTemplates:
    - metadata:
        name: data
      spec:
        accessModes: [ "{{ .Values.postgress.persistence.accessMode }}" ]
        resources:
          requests:
            storage: {{ .Values.postgress.persistence.size }}
        storageClassName: {{ .Values.postgress.persistence.storageClass }}
