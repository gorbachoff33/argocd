persistence:
  enabled: true
  size: 10Gi
  storageClass: local-path
  accessModes:
    - ReadWriteOnce

nfs:
  path: /exports
  mountOptions:
    - nolock

storageClass:
  create: true
  name: nfs-postgresql
  defaultClass: false
